import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Listing photos come from many hosts; plain <img> tags are used, so no image domains needed.
};

export default nextConfig;
